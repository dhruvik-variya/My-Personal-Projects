#include<stdio.h>

main()
{
	int i,j;
	
	for(i=1;i<=12;i++)
	{
		for(j=1;j<=12;j++)
		{
			if(j==6 || j==7 || i==6 || i==7  ||
				i==1 && (j>=8 && j<=12) || 
			    i==2 && (j>=8 && j<=12) || 
			    i==11 && (j>=1 && j<=5) ||
			    i==12 && (j>=1 && j<=5) ||
			    j==1 && (i>=1 && i<=5) || 
			    j==2 && (i>=1 && i<=5) ||
			    j==11 && (i>=8 && i<=12) ||
			    j==12 && (i>=8 && i<=12))
			{
				printf("* ");
			}
			else if(i==4 && ( j>=4 && j<=4 ||j>=9 && j<=9 ) ||
					i==9 && ( j>=4 && j<=4 ||j>=9 && j<=9) )
			{
				printf("* ");
			}
			else
			{
				printf("  ");
			}
		}
		printf("\n");
	}
}
